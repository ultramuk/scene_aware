function(get_library_filename LIBRARY_NAME OUTPUT_FILENAME LIBRARY_TYPE)
    if(${LIBRARY_TYPE} STREQUAL "STATIC")
        if(WIN32)
            set(${OUTPUT_FILENAME} "${LIBRARY_NAME}.lib" PARENT_SCOPE)
        else()
            set(${OUTPUT_FILENAME} "lib${LIBRARY_NAME}.a" PARENT_SCOPE)
        endif()
    else()
        if(WIN32)
            set(${OUTPUT_FILENAME} "${LIBRARY_NAME}.dll" PARENT_SCOPE)
        elseif(APPLE)
            set(${OUTPUT_FILENAME} "${LIBRARY_NAME}.dylib" PARENT_SCOPE)
        else()
            set(${OUTPUT_FILENAME} "lib${LIBRARY_NAME}.so" PARENT_SCOPE)
        endif()
    endif()
endfunction()
